# Python dependencies for development (all dependencies)
# This file now includes both core and ML framework dependencies
# For lighter installs, see:
# - requirements-test-core.txt (lightweight core testing)  
# - requirements-test-ml.txt (includes ML frameworks)

# Include all ML framework dependencies
-r requirements-test-ml.txt

# Additional dev-only tools
pre-commit
